<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    card_view:cardCornerRadius="16dp"
    card_view:cardElevation="8dp"
    card_view:cardBackgroundColor="@color/card_background_color">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Title (Day x) -->
        <TextView
            android:id="@+id/dayTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Day 1"
            android:textColor="@color/primary_text_color"
            android:textStyle="bold"
            android:textSize="18sp" />

        <!-- Tip Heading -->
        <TextView
            android:id="@+id/tipHeading"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Spend 15 minutes outdoors"
            android:textColor="@color/primary_text_color"
            android:textStyle="bold"
            android:textSize="16sp"
            android:layout_marginTop="4dp" />

        <!-- Image -->
        <ImageView
            android:id="@+id/imageView"
            android:layout_width="match_parent"
            android:layout_height="200dp"
            android:layout_marginTop="8dp"
            android:scaleType="centerCrop"
            android:src="@drawable/default_image" />

        <!-- Tip Description -->
        <TextView
            android:id="@+id/tipDescription"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Put some time on your calendar to take a walk outside or maybe even take a meeting while you're strolling through your neighborhood."
            android:textColor="@color/secondary_text_color"
            android:textSize="14sp"
            android:layout_marginTop="8dp" />
    </LinearLayout>

</androidx.cardview.widget.CardView>
